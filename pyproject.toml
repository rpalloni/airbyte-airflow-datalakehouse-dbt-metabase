[project]
name = "airbyte-airflow-datalakehouse-dbt-metabase"
version = "0.1.0"
description = "end-to-end data pipeline using Airbyte, Airflow, Minio, Nessie, Dremio, dbt, Metabase"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "dbt-core>=1.11.7",
    "dbt-dremio>=1.10.0",
]
